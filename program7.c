#include <stdio.h>

int main()
{
    int num1 = 25, num2 = 40;

    if (num1 > num2)
    {
        printf("%d is the maximum number.\n", num1);
    }
    else
    {
        printf("%d is the maximum number.\n", num2);
    }

    return 0;
}
